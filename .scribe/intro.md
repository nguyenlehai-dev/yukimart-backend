# Introduction

Hailn Core API - RESTful API cho quản lý xác thực (Auth), người dùng (User) và bài viết (Post). Sử dụng Laravel Sanctum để xác thực Bearer token.

<aside>
    <strong>Base URL</strong>: <code>http://localhost</code>
</aside>

    Tài liệu này cung cấp thông tin chi tiết để tích hợp và sử dụng Hailn Core API.

    <aside>Khi cuộn xuống, bạn sẽ thấy ví dụ code (Bash, JavaScript, PHP) cho mỗi endpoint. Có thể chọn ngôn ngữ bằng tab góc phải hoặc menu điều hướng trên mobile.</aside>

